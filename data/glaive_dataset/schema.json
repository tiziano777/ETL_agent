{
  "type": "object",
  "properties": {
    "prompt": {
      "type": "string",
      "description": "The prompt given to the language model."
    },
    "response": {
      "type": "string",
      "description": "The response generated by the language model."
    }
  },
  "required": [
    "prompt",
    "response"
  ],
  "description": "Schema for a prompt and response pair."
}