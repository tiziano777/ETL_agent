{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "The prompt presented to the model."
      },
      "response": {
        "type": "string",
        "description": "The model's response to the prompt."
      }
    },
    "required": [
      "prompt",
      "response"
    ]
  }
}